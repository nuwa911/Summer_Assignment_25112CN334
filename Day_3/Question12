// Find LCM of two numbers

#include<stdio.h>
int main(){
int a,b,x,y,t,g;
printf("Enter the numbers: ");
scanf("%d%d",&a,&b);
x=a;
y=b;
while(y!=0){
t=y;
y=x%y;
x=t;
}
g=x;
printf("%d",(a*b)/g);
return 0;
}
