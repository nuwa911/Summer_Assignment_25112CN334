// Check whether a number is palindrome

#include<stdio.h>
int main(){
int n,t,r=0;
printf("Enter the number");
scanf("%d",&n);
t=n;
while(n>0){
r=r*10+n%10;
n/=10;
}
if(t==r)
printf("Palindrome");
else
printf("Not Palindrome");
return 0;
}
