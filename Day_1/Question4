//Question 4. Count the Digit of a number
 
#include <stdio.h>

int main() {
int n;
int count = 0;
printf("Enter a number ");
scanf("%d", &n);
if(n < 0) n = -n;
if (n == 0) {
count = 1;
} else{
for (count = 0; n != 0; n /= 10) {
count++; }}
printf("Number of digits: %d\n", count);
return 0;
}
