//Write Function for Palindrome

#include<stdio.h>
int palindrome(int n){
int t=n,r=0;
while(n>0){
r=r*10+n%10;
n/=10;
}
return t==r;
}
int main(){
int n;
scanf("%d",&n);
if(palindrome(n))
printf("Palindrome");
else
printf("Not Palindrome");
return 0;
}
